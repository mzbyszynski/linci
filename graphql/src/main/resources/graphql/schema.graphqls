type Query {
    characterById(id: ID): Character
}

type Mutation {
    addCharacter(character: CharacterInput!): Character!
}

type Character {
    id: ID
    name: String
}

input CharacterInput {
    name: String
}